import { User as AuthUser, LoginCredentials, RegisterCredentials, AuthResponse } from './auth';
import { DoctorSearchResult, SearchParams, AvailableTimeSlot } from './appointment';

export type { AuthUser, DoctorSearchResult, SearchParams, AvailableTimeSlot, LoginCredentials, RegisterCredentials, AuthResponse };
